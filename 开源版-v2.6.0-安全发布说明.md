# 工业机器人语音控制示例 v2.6.0-open-source

## 安全发布边界

本仓库是可复用的工业语音控制参考实现，不包含任何生产现场机器人地址、寄存器映射、动作名称、工艺顺序或运行日志。

1. 默认 `ROBOT_CONTROL_ENABLED=0`，程序不会连接或向机器人下发动作。
2. 默认机器人地址为 `127.0.0.1`，命令寄存器为示例 `R40001`，本地参考服务使用 `127.0.0.1:1502`。
3. 示例工序一到示例工序五仅用于验证语音、界面和 Modbus 控制链路，不代表任何真实工艺。
4. 现场参数必须保存在未纳入版本控制的 `robot_config.local.env` 中。
5. 公开仓库不得提交生产 IP、寄存器、动作值、控制器项目、日志、部署报告或现场截图。

## 启用私有现场测试

1. 复制 `robot_config.example.env` 为 `robot_config.local.env`。
2. 在私有文件中填写经过批准的地址、端口和寄存器；真实动作映射留在受限的私有部署分支或控制器项目中。
3. 保持 `ROBOT_CONTROL_ENABLED=0`，完成低速、空载、急停和互锁验收后才可设为 `1`。
4. 验证 PLC、控制器和独立安全系统仍负责急停、安全门、碰撞、软限位、占用和工艺互锁。

## 保留的安全机制

- 低置信度、低信噪比、否定语义和非白名单值不会触发下发。
- 每条动作最多执行一次 Modbus 写入；超时或状态不明时绝不自动重发。
- 协议确认后控制台立即释放，不把它误判为机器人动作完成。
- 工程师诊断中心只管理本地诊断视图，不提供运动控制能力。
